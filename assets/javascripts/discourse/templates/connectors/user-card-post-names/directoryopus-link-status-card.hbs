{{#if siteSettings.directoryopus_enabled}}
	{{#if opusLinkAnything}}
		<div class="directoryopus-link-status">
			{{#if opusLinkClass}}
				<span class="{{opusLinkClass}}">{{fa-icon opusLinkIcon}} {{opusLinkText}}</span>
			{{/if}}
		</div>
	{{/if}}
{{/if}}
